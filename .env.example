# Database(postgres 與 rust-api 共用,docker-compose 從這裡讀)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=demo

# Composed for sqlx-cli + 本機 cargo run(host 上連 docker postgres 用 localhost)
# 必須對齊上面 POSTGRES_*
DATABASE_URL=postgres://postgres:postgres@localhost:5432/demo

# Admin
ADMIN_TOKEN=replace-with-strong-random-secret

# Logging
RUST_LOG=info,rust_api=debug

# Binance upstream
BINANCE_REST_BASE=https://api.binance.com
BINANCE_WS_BASE=wss://stream.binance.com:9443

# K 線 ingestion allowlist:逗號分隔 symbol(後端 trim + uppercase + dedupe)。
# 本階段 interval 固定 `1m`。未設此變數 → 用 code default(下方同 6 個)。
# 若所有項 trim 後皆空 → app 拒絕啟動。
BINANCE_KLINE_SYMBOLS=BTCUSDT,ETHUSDT,BNBUSDT,SOLUSDT,DOGEUSDT,SHIBUSDT
